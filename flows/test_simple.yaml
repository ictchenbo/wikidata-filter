name: TextProvider test
description:
arguments: 0
consts:
  path: $PATH

#loader: RandomGenerator(5)
loader: Text(path, sep=';')

nodes:
  print: Print
  count: Count

processor: Chain(print, count)
